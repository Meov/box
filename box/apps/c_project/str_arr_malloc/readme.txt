use this to produce any sized string arr via parameter "argc" "argv"
for example:

	./test zhang san li si wang wu 
	
	it will created the string arr automatically as you entered without careing numbers of string
